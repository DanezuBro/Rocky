@model IEnumerable<Rocky.Models.ApplicationType>


<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Application Type List</h2>
        </div>

        <div class="col-6 text-end">
            <a asp-action="Create" asp-controller="ApplicationType" class="btn btn-primary"><i class="fa-solid fa-plus"></i> Create New Application Type</a>
        </div>
    </div>

    <br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%;">
            <thead>
                <tr>
                    <th scope="col" class="text-center">Application Type Name</th>
                    <th scope="col" class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var appType in Model)
                {
                    <tr>
                        <td width="80%" class="text-center">@appType.Name</td>
                        <td class="align-items-center">
                            <div  class="text-center" role="group">
                                <a asp-controller="ApplicationType" asp-action="Edit" asp-route-id="@appType.Id" class="btn btn-primary mx-2"><i class="fa-solid fa-pen-to-square"></i></a>
                                <a asp-controller="ApplicationType" asp-action="Delete" asp-route-id="@appType.Id" class="btn btn-danger mx-2"><i class="fa-solid fa-trash-can"></i></a>
                            </div>
                        </td>

                    </tr>
                }

            </tbody>
        </table>
    }
    else
    {
        <p>No Application Type exists.</p>
    }

</div>